name: CI Feature Branch

on:
  push:
    branches:
      - 'feature/**'

  pull_request:
    branches:
      - 'feature/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do código
        uses: actions/checkout@v4

      - name: Exemplo de execução
        run: echo "Rodando workflow para branch feature"